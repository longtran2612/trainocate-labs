eureka:
  client:
    enabled: false

spring:
  datasource:
    url: jdbc:postgresql://postgresql:5432/limit_db
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:123456}
  kafka:
    bootstrap-servers: kafka:9092
