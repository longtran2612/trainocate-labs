server:
  port: 8084

spring:
  application:
    name: limit-service
  datasource:
    url: jdbc:postgresql://localhost:5432/limit_db
    username: postgres
    password: 123456
  sql:
    init:
      mode: always
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
